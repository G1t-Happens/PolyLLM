# Add your own .env file in the root folder with your own database credentials following the schema below.
DB_URL=jdbc:<database_type>://<host>:<port>/<database_name>
DB_USER=<user>
DB_PASSWORD=<password>
